# LOC Counter

言語別コード行数をカウント・統計表示する CLI ツール

## 概要

プロジェクト内のソースコードを言語別に分類し、行数（Lines of Code）を集計します。コード行、コメント行、空行を区別してカウントします。

## 機能

- ファイル拡張子から言語を自動判定
- コード行、コメント行、空行を区別してカウント
- 言語別の集計と統計表示
- JSON/テキスト形式での出力
- 除外パターンによるフィルタリング
- ファイル単位の詳細表示

## 対応言語

- Rust (.rs)
- TypeScript/JavaScript (.ts, .tsx, .js, .jsx)
- Python (.py)
- Go (.go)
- Java (.java)
- C/C++ (.c, .h, .cpp, .hpp)
- C# (.cs)
- HTML/CSS (.html, .css, .scss)
- JSON/YAML/TOML (.json, .yaml, .yml, .toml)
- Markdown (.md)
- Shell (.sh, .bash, .zsh)
- Ruby (.rb)
- PHP (.php)
- Swift (.swift)
- Kotlin (.kt)
- SQL (.sql)
- Vue/Svelte (.vue, .svelte)
- XML (.xml)

## 使い方

```bash
# カレントディレクトリをスキャン
loc-counter

# 指定ディレクトリをスキャン
loc-counter -s ~/projects/myapp

# 除外パターンを指定
loc-counter -e "node_modules,target,.git,vendor"

# テキスト形式で出力
loc-counter -o text

# ファイル単位で詳細表示
loc-counter --by-file

# コード行数でソート
loc-counter --sort code
```

## オプション

| オプション   | 短縮形 | 説明                              | デフォルト                   |
| ------------ | ------ | --------------------------------- | ---------------------------- |
| `--scan-dir` | `-s`   | スキャン対象ディレクトリ          | カレントディレクトリ         |
| `--exclude`  | `-e`   | 除外パターン（カンマ区切り）      | node_modules,target,.git,... |
| `--output`   | `-o`   | 出力形式（text/json）             | json                         |
| `--sort`     | -      | ソート順（lines/files/name/code） | lines                        |
| `--by-file`  | -      | ファイル単位で詳細表示            | false                        |

## 出力形式

### JSON

```json
{
  "summary": {
    "total_files": 45,
    "total_lines": 12500,
    "code_lines": 9800,
    "comment_lines": 1500,
    "blank_lines": 1200
  },
  "by_language": [
    {
      "language": "Rust",
      "files": 25,
      "code": 6500,
      "comments": 1000,
      "blanks": 500,
      "lines": 8000,
      "percentage": 64.0
    }
  ]
}
```

### Text

```
📊 Summary
────────────────────────────────────────
  Total Files:      45
  Total Lines:  12,500
  Code Lines:    9,800
  Comments:      1,500
  Blank Lines:   1,200

📈 By Language
──────────────────────────────────────────────────────────────────────────────────────────
Language        Files       Code   Comments     Blanks      Total        %
──────────────────────────────────────────────────────────────────────────────────────────
Rust               25      6,500      1,000        500      8,000    64.0%
TypeScript         15      2,800        400        300      3,500    28.0%
```

## ビルド

```bash
cd tools/loc-counter
cargo build --release
```

## テスト

```bash
cd tools/loc-counter
cargo test
```

## ライセンス

MIT
