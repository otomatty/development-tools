# PRD: 設定ページ

**Document Version:** 1.0  
**Last Updated:** 2024-11-26  
**Status:** Draft  

---

## 1. 概要（Executive Summary）

### 1.1 プロダクトビジョン

アプリケーションの動作をユーザーが自由にカスタマイズできる設定ページを実装する。アカウント管理、通知制御、データ同期、外観調整など、ユーザー体験を最適化するための各種設定を提供する。

### 1.2 目標

| 目標 | 指標 | ターゲット |
|------|------|-----------|
| ユーザー体験向上 | 設定変更後の満足度 | 90%以上 |
| 操作性 | 設定変更完了時間 | 10秒以内 |
| 信頼性 | 設定保存成功率 | 99.9%以上 |

### 1.3 対象ユーザー

- **プライマリ**: 全アプリユーザー
- **セカンダリ**: パワーユーザー（詳細設定を活用）

---

## 2. 技術スタック

| レイヤー | 技術 | 理由 |
|---------|------|------|
| **フロントエンド** | Leptos (Rust/WASM) | 既存アプリとの統合、型安全性 |
| **バックエンド** | Tauri 2.0 + Rust | デスクトップアプリ、セキュリティ |
| **データベース** | SQLite | ローカル優先、設定の永続化 |
| **通知** | Tauri Notification Plugin | OSネイティブ通知対応 |
| **スタイリング** | Tailwind CSS | 既存スタイルとの統合 |

---

## 3. 機能要件

### 3.1 設定ページ全体構成

#### 3.1.1 UIレイアウト

アコーディオン形式で各カテゴリを展開・折りたたみ可能にする。

```
┌─────────────────────────────────────────────────────────────────────────┐
│ [Sidebar - 60px]  │                    SETTINGS                         │
│                   │                                                      │
│ ┌───────────────┐ │  ┌──────────────────────────────────────────────┐   │
│ │ 🏠 Home       │ │  │ ▼ アカウント設定                             │   │
│ └───────────────┘ │  │   ┌────────────────────────────────────────┐ │   │
│ ┌───────────────┐ │  │   │ GitHubアカウント: @username            │ │   │
│ │ 🔧 Tools      │ │  │   │ [ログアウト]                          │ │   │
│ └───────────────┘ │  │   └────────────────────────────────────────┘ │   │
│ ┌───────────────┐ │  └──────────────────────────────────────────────┘   │
│ │ ⚙️ Settings   │ │                                                      │
│ │   (active)    │ │  ┌──────────────────────────────────────────────┐   │
│ └───────────────┘ │  │ ▶ 通知設定                                   │   │
│                   │  └──────────────────────────────────────────────┘   │
│                   │                                                      │
│                   │  ┌──────────────────────────────────────────────┐   │
│                   │  │ ▶ 同期設定                                   │   │
│                   │  └──────────────────────────────────────────────┘   │
│                   │                                                      │
│                   │  ┌──────────────────────────────────────────────┐   │
│                   │  │ ▶ 外観設定                                   │   │
│                   │  └──────────────────────────────────────────────┘   │
│                   │                                                      │
│                   │  ┌──────────────────────────────────────────────┐   │
│                   │  │ ▶ データ管理                                 │   │
│                   │  └──────────────────────────────────────────────┘   │
│                   │                                                      │
│                   │  ┌──────────────────────────────────────────────┐   │
│                   │  │ ▶ アプリケーション設定                        │   │
│                   │  └──────────────────────────────────────────────┘   │
│                   │                                                      │
│                   │  ┌──────────────────────────────────────────────┐   │
│                   │  │ [全ての設定をリセット]                        │   │
│                   │  └──────────────────────────────────────────────┘   │
│                   │                                                      │
│                   │  ┌──────────────────────────────────────────────┐   │
│                   │  │ バージョン: 1.0.0                             │   │
│                   │  └──────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
```

#### 3.1.2 設定カテゴリ一覧

| カテゴリ | 説明 | 優先度 |
|---------|------|--------|
| アカウント設定 | GitHubアカウント管理 | P0 |
| 通知設定 | 通知の表示方法・種類 | P0 |
| 同期設定 | GitHub統計の同期間隔 | P0 |
| 外観設定 | アニメーションON/OFF | P1 |
| データ管理 | キャッシュ・データ管理 | P1 |
| アプリケーション設定 | バージョン情報等 | P2 |

---

### 3.2 アカウント設定

#### 3.2.1 ユーザーストーリー

> ユーザーとして、接続中のGitHubアカウント情報を確認し、必要に応じてログアウトしたい。

#### 3.2.2 機能詳細

| 機能 | 説明 | 優先度 |
|------|------|--------|
| アカウント情報表示 | アバター、ユーザー名、GitHub ID表示 | P0 |
| ログアウト | トークンのみ削除（ユーザーデータは保持） | P0 |
| トークンリフレッシュ | 手動でトークンを更新 | P1 |

#### 3.2.3 ログアウト処理フロー

```
┌─────────┐     ┌─────────────┐     ┌─────────────┐
│  User   │     │   Frontend  │     │   Backend   │
└────┬────┘     └──────┬──────┘     └──────┬──────┘
     │                 │                   │
     │  ログアウト      │                   │
     │  クリック        │                   │
     │────────────────>│                   │
     │                 │                   │
     │                 │  確認ダイアログ     │
     │<────────────────│                   │
     │                 │                   │
     │  確認           │                   │
     │────────────────>│                   │
     │                 │                   │
     │                 │  logout_user()    │
     │                 │──────────────────>│
     │                 │                   │
     │                 │                   │ トークン削除
     │                 │                   │ (データ保持)
     │                 │                   │
     │                 │  Success          │
     │                 │<──────────────────│
     │                 │                   │
     │  ホーム画面へ    │                   │
     │<────────────────│                   │
     │                 │                   │
```

#### 3.2.4 UIコンポーネント

```
┌─────────────────────────────────────────────────────────────┐
│ ▼ アカウント設定                                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────┐  GitHubアカウント                                 │
│  │Avatar│  @username                                        │
│  │ 64px │  GitHub ID: 12345678                              │
│  └──────┘  接続日: 2024-11-26                               │
│                                                             │
│  ┌─────────────────────┐  ┌─────────────────────┐          │
│  │ 🔄 トークンを更新    │  │ 🚪 ログアウト       │          │
│  └─────────────────────┘  └─────────────────────┘          │
│                                                             │
│  ※ログアウトしてもXP・バッジ・統計データは保持されます       │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

### 3.3 通知設定

#### 3.3.1 ユーザーストーリー

> ユーザーとして、どの通知を受け取るか、どこに表示するかを細かく設定したい。

#### 3.3.2 機能詳細

| 機能 | 説明 | 優先度 |
|------|------|--------|
| 通知方法選択 | アプリ内トースト / OSネイティブ / 両方 / なし | P0 |
| 一括ON/OFF | 全通知の一括制御 | P0 |
| 個別通知設定 | 通知種類ごとのON/OFF | P0 |

#### 3.3.3 通知種類

| 通知種類 | 説明 | デフォルト |
|---------|------|-----------|
| XP獲得通知 | XPを獲得した時の通知 | ON |
| レベルアップ通知 | レベルが上がった時の演出・通知 | ON |
| バッジ獲得通知 | 新しいバッジを獲得した時の通知 | ON |
| ストリーク更新通知 | ストリークが更新された時の通知 | ON |
| ストリークマイルストーン通知 | ストリークマイルストーン達成時 | ON |

#### 3.3.4 通知方法オプション

| オプション | 説明 |
|-----------|------|
| アプリ内のみ | アプリ内トースト通知のみ表示 |
| OSネイティブのみ | システム通知のみ表示 |
| 両方 | アプリ内 + システム通知 |
| 通知なし | 全ての通知を無効化 |

#### 3.3.5 UIコンポーネント

```
┌─────────────────────────────────────────────────────────────┐
│ ▼ 通知設定                                                  │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  通知方法                                                   │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ ○ アプリ内のみ                                       │   │
│  │ ○ OSネイティブのみ                                   │   │
│  │ ● 両方                                              │   │
│  │ ○ 通知なし                                          │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ─────────────────────────────────────────────────────     │
│                                                             │
│  通知の種類                                                 │
│                                                             │
│  ┌──────────────────────────────────┐  [全てON] [全てOFF]  │
│  │                                  │                      │
│  │  XP獲得通知              [ON/OFF]│                      │
│  │  レベルアップ通知         [ON/OFF]│                      │
│  │  バッジ獲得通知           [ON/OFF]│                      │
│  │  ストリーク更新通知        [ON/OFF]│                      │
│  │  ストリークマイルストーン  [ON/OFF]│                      │
│  │                                  │                      │
│  └──────────────────────────────────┘                      │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

### 3.4 同期設定

#### 3.4.1 ユーザーストーリー

> ユーザーとして、GitHub統計の同期タイミングを制御したい。データ通信量やバッテリー消費を最適化するために。

#### 3.4.2 機能詳細

| 機能 | 説明 | 優先度 |
|------|------|--------|
| 自動同期間隔 | 同期間隔の選択 | P0 |
| バックグラウンド同期 | バックグラウンドでの同期有効化 | P0 |
| 起動時同期 | アプリ起動時に自動同期 | P1 |
| 手動同期 | 今すぐ同期ボタン | P0 |
| 最終同期日時表示 | 最後に同期した日時を表示 | P1 |

#### 3.4.3 同期間隔オプション

| オプション | 説明 | デフォルト |
|-----------|------|-----------|
| 5分 | 頻繁に更新 | - |
| 15分 | 比較的頻繁 | - |
| 30分 | 標準的 | - |
| 1時間 | 控えめ | ✓ |
| 3時間 | まれに | - |
| 手動のみ | 自動同期しない | - |

#### 3.4.4 UIコンポーネント

```
┌─────────────────────────────────────────────────────────────┐
│ ▼ 同期設定                                                  │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  自動同期間隔                                               │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ [▼ 1時間                                         ]  │   │
│  │     ├─ 5分                                          │   │
│  │     ├─ 15分                                         │   │
│  │     ├─ 30分                                         │   │
│  │     ├─ 1時間 ✓                                      │   │
│  │     ├─ 3時間                                        │   │
│  │     └─ 手動のみ                                     │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ─────────────────────────────────────────────────────     │
│                                                             │
│  バックグラウンド同期                         [ON/OFF]      │
│  アプリがバックグラウンドにある時も同期を続ける              │
│                                                             │
│  起動時に同期                                 [ON/OFF]      │
│  アプリ起動時に自動的に同期を実行する                       │
│                                                             │
│  ─────────────────────────────────────────────────────     │
│                                                             │
│  最終同期: 2024-11-26 15:30:45                              │
│                                                             │
│  ┌─────────────────────┐                                   │
│  │ 🔄 今すぐ同期        │                                   │
│  └─────────────────────┘                                   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

### 3.5 外観設定

#### 3.5.1 ユーザーストーリー

> ユーザーとして、アニメーション効果をON/OFFしたい。パフォーマンスやアクセシビリティの観点で調整するために。

#### 3.5.2 機能詳細

| 機能 | 説明 | 優先度 |
|------|------|--------|
| アニメーションON/OFF | 全アニメーション効果の一括制御 | P0 |

#### 3.5.3 アニメーション対象

| アニメーション | 説明 |
|---------------|------|
| XP獲得アニメーション | XP獲得時の浮き上がり表示 |
| レベルアップ演出 | パーティクル爆発 + モーダル |
| バッジ獲得演出 | シャイン + モーダル |
| プログレスバー | スムーズなバー伸長 |
| ストリーク炎 | ストリーク継続時の火のアニメーション |

#### 3.5.4 UIコンポーネント

```
┌─────────────────────────────────────────────────────────────┐
│ ▼ 外観設定                                                  │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  アニメーション効果                           [ON/OFF]      │
│  XP獲得、レベルアップ、バッジ獲得時の                       │
│  アニメーション効果を有効にする                             │
│                                                             │
│  ※OFFにするとパフォーマンスが向上する場合があります         │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

### 3.6 データ管理

#### 3.6.1 ユーザーストーリー

> ユーザーとして、キャッシュのクリアやデータのエクスポート・リセットを行いたい。

#### 3.6.2 機能詳細

| 機能 | 説明 | 優先度 |
|------|------|--------|
| キャッシュクリア | コントリビューショングラフ等のキャッシュ削除 | P0 |
| データエクスポート | 統計データをJSONでエクスポート | P1 |
| 全データリセット | XP/レベル/バッジ/統計を全て初期化 | P1 |
| データベース情報 | DBサイズ・パス表示 | P2 |

#### 3.6.3 データリセット処理フロー

```
┌─────────┐     ┌─────────────┐     ┌─────────────┐
│  User   │     │   Frontend  │     │   Backend   │
└────┬────┘     └──────┬──────┘     └──────┬──────┘
     │                 │                   │
     │  リセットボタン  │                   │
     │  クリック        │                   │
     │────────────────>│                   │
     │                 │                   │
     │                 │  警告ダイアログ     │
     │                 │  「この操作は取り消せません」
     │<────────────────│                   │
     │                 │                   │
     │  確認入力        │                   │
     │  "RESET"と入力   │                   │
     │────────────────>│                   │
     │                 │                   │
     │                 │  reset_all_data() │
     │                 │──────────────────>│
     │                 │                   │
     │                 │                   │ user_stats削除
     │                 │                   │ badges削除
     │                 │                   │ xp_history削除
     │                 │                   │ challenges削除
     │                 │                   │ activity_cache削除
     │                 │                   │
     │                 │  Success          │
     │                 │<──────────────────│
     │                 │                   │
     │  完了通知        │                   │
     │<────────────────│                   │
     │                 │                   │
```

#### 3.6.4 エクスポートデータ形式

```json
{
  "exportedAt": "2024-11-26T15:30:00Z",
  "version": "1.0",
  "user": {
    "githubId": 12345678,
    "username": "example"
  },
  "stats": {
    "totalXp": 5000,
    "currentLevel": 15,
    "currentStreak": 10,
    "longestStreak": 30,
    "totalCommits": 500,
    "totalPrs": 50,
    "totalReviews": 100,
    "totalIssues": 30
  },
  "badges": [
    {
      "badgeId": "first_blood",
      "badgeType": "milestone",
      "earnedAt": "2024-01-01T00:00:00Z"
    }
  ],
  "xpHistory": [
    {
      "actionType": "commit",
      "xpAmount": 10,
      "createdAt": "2024-11-26T10:00:00Z"
    }
  ]
}
```

#### 3.6.5 UIコンポーネント

```
┌─────────────────────────────────────────────────────────────┐
│ ▼ データ管理                                                │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  キャッシュ                                                 │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ キャッシュサイズ: 2.5 MB                              │   │
│  │                                                       │   │
│  │ ┌─────────────────────┐                              │   │
│  │ │ 🗑️ キャッシュをクリア │                              │   │
│  │ └─────────────────────┘                              │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ─────────────────────────────────────────────────────     │
│                                                             │
│  データエクスポート                                          │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 統計データをJSON形式でエクスポートします               │   │
│  │                                                       │   │
│  │ ┌─────────────────────┐                              │   │
│  │ │ 📥 データをエクスポート │                            │   │
│  │ └─────────────────────┘                              │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ─────────────────────────────────────────────────────     │
│                                                             │
│  データリセット                                              │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ ⚠️ 全てのXP、バッジ、統計データを削除します           │   │
│  │ この操作は取り消せません                              │   │
│  │                                                       │   │
│  │ ┌─────────────────────┐                              │   │
│  │ │ 🗑️ 全データをリセット │ (赤色ボタン)                │   │
│  │ └─────────────────────┘                              │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ─────────────────────────────────────────────────────     │
│                                                             │
│  データベース情報                                            │
│  パス: ~/Library/Application Support/development-tools/     │
│  サイズ: 5.2 MB                                             │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

### 3.7 アプリケーション設定

#### 3.7.1 ユーザーストーリー

> ユーザーとして、アプリのバージョン情報を確認したい。

#### 3.7.2 機能詳細

| 機能 | 説明 | 優先度 |
|------|------|--------|
| バージョン情報 | アプリバージョン表示 | P0 |
| ビルド情報 | ビルド日時・コミットハッシュ | P2 |
| ライセンス情報 | 使用ライブラリのライセンス表示 | P2 |

#### 3.7.3 UIコンポーネント

```
┌─────────────────────────────────────────────────────────────┐
│ ▼ アプリケーション設定                                       │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Development Tools                                          │
│                                                             │
│  バージョン: 1.0.0                                          │
│  ビルド日時: 2024-11-26                                     │
│  Tauri: 2.0.0                                               │
│  Leptos: 0.6.0                                              │
│                                                             │
│  ┌─────────────────────┐                                   │
│  │ 📄 ライセンス情報     │                                   │
│  └─────────────────────┘                                   │
│                                                             │
│  ┌─────────────────────┐                                   │
│  │ 🐙 GitHubリポジトリ   │                                   │
│  └─────────────────────┘                                   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

### 3.8 設定リセット

#### 3.8.1 ユーザーストーリー

> ユーザーとして、全ての設定をデフォルトに戻したい。

#### 3.8.2 機能詳細

| 機能 | 説明 | 優先度 |
|------|------|--------|
| 設定リセット | 全設定をデフォルト値に戻す | P0 |

#### 3.8.3 リセット対象

| 設定項目 | デフォルト値 |
|---------|-------------|
| 通知方法 | 両方（アプリ内 + OS） |
| 通知種類 | 全てON |
| 自動同期間隔 | 1時間 |
| バックグラウンド同期 | ON |
| 起動時同期 | ON |
| アニメーション | ON |

#### 3.8.4 UIコンポーネント

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│  ┌───────────────────────────────────────────────────┐     │
│  │ ⚙️ 全ての設定をリセット                             │     │
│  │ 設定をデフォルト値に戻します（データは削除されません）│     │
│  └───────────────────────────────────────────────────┘     │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 4. データベース設計

### 4.1 設定テーブル

```sql
-- ユーザー設定
CREATE TABLE user_settings (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER UNIQUE NOT NULL,
    
    -- 通知設定
    notification_method TEXT DEFAULT 'both', -- 'app_only' | 'os_only' | 'both' | 'none'
    notify_xp_gain BOOLEAN DEFAULT 1,
    notify_level_up BOOLEAN DEFAULT 1,
    notify_badge_earned BOOLEAN DEFAULT 1,
    notify_streak_update BOOLEAN DEFAULT 1,
    notify_streak_milestone BOOLEAN DEFAULT 1,
    
    -- 同期設定
    sync_interval_minutes INTEGER DEFAULT 60, -- 5, 15, 30, 60, 180, 0 (manual)
    background_sync BOOLEAN DEFAULT 1,
    sync_on_startup BOOLEAN DEFAULT 1,
    
    -- 外観設定
    animations_enabled BOOLEAN DEFAULT 1,
    
    -- メタデータ
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
);

-- インデックス
CREATE INDEX idx_user_settings_user_id ON user_settings(user_id);
```

### 4.2 デフォルト値定数

```rust
pub mod settings_defaults {
    pub const NOTIFICATION_METHOD: &str = "both";
    pub const NOTIFY_XP_GAIN: bool = true;
    pub const NOTIFY_LEVEL_UP: bool = true;
    pub const NOTIFY_BADGE_EARNED: bool = true;
    pub const NOTIFY_STREAK_UPDATE: bool = true;
    pub const NOTIFY_STREAK_MILESTONE: bool = true;
    pub const SYNC_INTERVAL_MINUTES: i32 = 60;
    pub const BACKGROUND_SYNC: bool = true;
    pub const SYNC_ON_STARTUP: bool = true;
    pub const ANIMATIONS_ENABLED: bool = true;
}
```

---

## 5. API設計

### 5.1 Tauriコマンド一覧

| コマンド | 説明 | 引数 | 戻り値 |
|---------|------|------|--------|
| `get_settings` | 設定を取得 | - | `UserSettings` |
| `update_settings` | 設定を更新 | `UserSettings` | `Result<()>` |
| `reset_settings` | 設定をリセット | - | `Result<UserSettings>` |
| `clear_cache` | キャッシュをクリア | - | `Result<ClearCacheResult>` |
| `export_data` | データをエクスポート | - | `Result<String>` (JSON) |
| `reset_all_data` | 全データをリセット | - | `Result<()>` |
| `get_database_info` | DB情報を取得 | - | `DatabaseInfo` |
| `trigger_sync` | 手動同期を実行 | - | `Result<SyncResult>` |

### 5.2 型定義

```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct UserSettings {
    pub notification_method: NotificationMethod,
    pub notify_xp_gain: bool,
    pub notify_level_up: bool,
    pub notify_badge_earned: bool,
    pub notify_streak_update: bool,
    pub notify_streak_milestone: bool,
    pub sync_interval_minutes: i32,
    pub background_sync: bool,
    pub sync_on_startup: bool,
    pub animations_enabled: bool,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(rename_all = "snake_case")]
pub enum NotificationMethod {
    AppOnly,
    OsOnly,
    Both,
    None,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct DatabaseInfo {
    pub path: String,
    pub size_bytes: u64,
    pub cache_size_bytes: u64,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ClearCacheResult {
    pub cleared_entries: i32,
    pub freed_bytes: u64,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ExportData {
    pub exported_at: String,
    pub version: String,
    pub user: ExportUser,
    pub stats: ExportStats,
    pub badges: Vec<ExportBadge>,
    pub xp_history: Vec<ExportXpEntry>,
}
```

---

## 6. コンポーネント設計

### 6.1 コンポーネント一覧

| コンポーネント | 説明 | 優先度 |
|---------------|------|--------|
| `SettingsPage` | 設定ページ全体 | P0 |
| `SettingsAccordion` | アコーディオンコンテナ | P0 |
| `SettingsSection` | 各設定セクション | P0 |
| `AccountSettings` | アカウント設定 | P0 |
| `NotificationSettings` | 通知設定 | P0 |
| `SyncSettings` | 同期設定 | P0 |
| `AppearanceSettings` | 外観設定 | P1 |
| `DataManagement` | データ管理 | P1 |
| `AppInfo` | アプリケーション情報 | P2 |
| `ToggleSwitch` | ON/OFFトグル | P0 |
| `SelectDropdown` | ドロップダウン選択 | P0 |
| `ConfirmDialog` | 確認ダイアログ | P0 |
| `DangerButton` | 危険な操作用ボタン | P0 |

### 6.2 状態管理

```rust
/// 設定ページの状態
#[derive(Clone)]
pub struct SettingsState {
    /// 現在の設定値
    pub settings: RwSignal<Option<UserSettings>>,
    /// 読み込み中フラグ
    pub loading: RwSignal<bool>,
    /// 保存中フラグ
    pub saving: RwSignal<bool>,
    /// エラーメッセージ
    pub error: RwSignal<Option<String>>,
    /// 展開中のアコーディオンセクション
    pub expanded_section: RwSignal<Option<SettingsSection>>,
}

#[derive(Clone, Copy, PartialEq, Eq)]
pub enum SettingsSection {
    Account,
    Notification,
    Sync,
    Appearance,
    DataManagement,
    AppInfo,
}
```

---

## 7. 開発フェーズ

### 7.1 フェーズ概要

| Phase | 内容 | 期間目安 |
|-------|------|---------|
| 1 | 基盤整備（DB、API、型定義） | 3日 |
| 2 | アカウント設定・ログアウト | 2日 |
| 3 | 通知設定 | 2日 |
| 4 | 同期設定 | 2日 |
| 5 | 外観設定 | 1日 |
| 6 | データ管理 | 3日 |
| 7 | アプリ情報・設定リセット | 1日 |
| 8 | UIポリッシュ・テスト | 2日 |

### 7.2 MVP定義

**Phase 1〜4** をMVPとする。

MVP完了時点で以下が動作する：
- 設定ページのアコーディオンUI
- アカウント情報表示・ログアウト
- 通知設定（方法・種類）
- 同期設定（間隔・バックグラウンド・手動同期）

---

## 8. テスト戦略

### 8.1 テストカテゴリ

| カテゴリ | ツール | 対象 |
|---------|--------|------|
| Unit Test | `cargo test` | 設定ロジック、デフォルト値 |
| Integration Test | `cargo test` | DB操作、API |
| UI Test | `wasm-bindgen-test` | コンポーネント |

### 8.2 テストケース例

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_default_settings() {
        let settings = UserSettings::default();
        assert_eq!(settings.notification_method, NotificationMethod::Both);
        assert_eq!(settings.sync_interval_minutes, 60);
        assert!(settings.animations_enabled);
    }

    #[test]
    fn test_reset_settings() {
        let mut settings = UserSettings::default();
        settings.sync_interval_minutes = 5;
        settings.animations_enabled = false;
        
        let reset = settings.reset();
        assert_eq!(reset.sync_interval_minutes, 60);
        assert!(reset.animations_enabled);
    }
}
```

---

## 9. 非機能要件

### 9.1 パフォーマンス

| 指標 | 目標 |
|------|------|
| 設定ページ描画 | 500ms以内 |
| 設定保存 | 200ms以内 |
| キャッシュクリア | 1秒以内 |
| データエクスポート | 3秒以内 |

### 9.2 ユーザビリティ

| 指標 | 目標 |
|------|------|
| 設定変更の即時反映 | 変更後すぐにUIに反映 |
| エラーメッセージ | 明確で具体的な説明 |
| 確認ダイアログ | 危険な操作前に必ず表示 |

---

## 10. 将来の拡張

### 10.1 Phase 2以降の機能

| 機能 | 説明 | 優先度 |
|------|------|--------|
| データインポート | エクスポートしたデータの復元 | P2 |
| テーマ切り替え | ダーク/ライト/システム追従 | P2 |
| アクセントカラー | カスタムカラー選択 | P3 |
| 言語設定 | 英語対応 | P3 |
| ウィンドウ設定 | サイズ・位置の記憶 | P3 |

---

## 11. 用語集

| 用語 | 説明 |
|------|------|
| アコーディオン | 展開・折りたたみ可能なUIパターン |
| トースト通知 | アプリ内に一時的に表示される通知 |
| OSネイティブ通知 | システムの通知センターに表示される通知 |
| バックグラウンド同期 | アプリが非アクティブ時でも行う同期 |

---

**Document History**

| Version | Date | Author | Changes |
|---------|------|--------|---------|
| 1.0 | 2024-11-26 | - | Initial draft |

